apiVersion: activity.miloapis.com/v1alpha1
kind: ActivityPolicy
metadata:
  name: test-invalid-name-hyphen-start
spec:
  resource:
    apiGroup: apps
    kind: Deployment
  auditRules:
    - name: "-create-rule"
      match: "verb == 'create'"
      summary: "{{ actor }} created deployment"
