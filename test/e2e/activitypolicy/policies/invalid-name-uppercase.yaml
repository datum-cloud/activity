apiVersion: activity.miloapis.com/v1alpha1
kind: ActivityPolicy
metadata:
  name: test-invalid-name-uppercase
spec:
  resource:
    apiGroup: apps
    kind: Deployment
  auditRules:
    - name: "CreateRule"
      match: "audit.verb == 'create'"
      summary: "{{ actor }} created deployment"
