apiVersion: activity.miloapis.com/v1alpha1
kind: ActivityPolicy
metadata:
  name: test-edge-case-hyphen-end
spec:
  resource:
    apiGroup: apps
    kind: Deployment
  auditRules:
    - name: "rule-name-"
      match: "audit.verb == 'create'"
      summary: "{{ actor }} created deployment"
