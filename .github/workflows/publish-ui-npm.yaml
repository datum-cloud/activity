name: Publish UI to NPM

on:
  push:

jobs:
  publish:
    uses: datum-cloud/actions/.github/workflows/publish-npm-package.yaml@fix/pnpm-package-path
    with:
      package-name: "@datum-cloud/activity-ui"
      package-path: ui
    secrets: inherit
