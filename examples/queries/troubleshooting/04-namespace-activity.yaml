apiVersion: activity.miloapis.com/v1alpha1
kind: AuditLogQuery
metadata:
  name: production-namespace-activity
spec:
  # Query the last 7 days
  startTime: "now-7d"
  endTime: "now"
  # Monitor specific namespace for troubleshooting
  filter: "objectRef.namespace == 'production'"
  limit: 200
