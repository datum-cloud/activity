apiVersion: activity.miloapis.com/v1alpha1
kind: AuditLogQuery
metadata:
  name: pci-namespace-audit
spec:
  # Query the last 7 days
  startTime: "now-7d"
  endTime: "now"
  # Audit all operations in PCI-DSS compliant namespace
  filter: "objectRef.namespace == 'pci-dss'"
  limit: 500
