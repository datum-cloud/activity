apiVersion: activity.miloapis.com/v1alpha1
kind: AuditLogQuery
metadata:
  name: all-write-operations
spec:
  # Query the last 7 days
  startTime: "now-7d"
  endTime: "now"
  # Comprehensive audit of all write operations
  filter: "verb in ['create', 'update', 'patch', 'delete']"
  limit: 500
