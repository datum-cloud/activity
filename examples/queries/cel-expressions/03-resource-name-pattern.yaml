apiVersion: activity.miloapis.com/v1alpha1
kind: AuditLogQuery
metadata:
  name: nginx-resources
spec:
  # Query the last 7 days
  startTime: "now-7d"
  endTime: "now"
  # Find resources with names starting with 'nginx'
  filter: "objectRef.name.startsWith('nginx')"
  limit: 100
