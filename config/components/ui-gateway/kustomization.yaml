apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Envoy Gateway routing for the Activity UI
# This component configures HTTPRoute rules to expose the UI and Activity API
# through an Envoy Gateway. Requires the ui component to be deployed.

resources:
  - httproute.yaml
  - backend-tls-policy.yaml
  - backendtrafficpolicy.yaml
