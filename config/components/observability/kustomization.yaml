apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

resources:
  # ServiceMonitors for metrics collection
  # Note: Vector and NATS use PodMonitors configured in their Helm charts
  - servicemonitors/activity-apiserver-servicemonitor.yaml
  - servicemonitors/clickhouse-servicemonitor.yaml

  # Alert rules and recording rules (generated from Jsonnet mixin)
  # Generate with: task observability:build-mixin
  - alerts/generated/activity-alerts.yaml
  - alerts/generated/activity-recordings.yaml

  # Grafana dashboards
  - dashboards/
