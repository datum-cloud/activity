apiVersion: v1
kind: Service
metadata:
  name: activity-ui
  namespace: activity-system
  labels:
    app: activity-ui
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: activity-ui
