apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Core Activity UI deployment
# For Envoy Gateway routing, also include the ui-gateway component.

resources:
  - deployment.yaml
  - service.yaml
  - rbac.yaml
