{
  "name": "@miloapis/activity-ui",
  "version": "0.0.0",
  "description": "UI components for Datum Cloud Activity API",
  "type": "module",
  "scripts": {
    "build": "rollup -c",
    "dev": "rollup -c -w",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:headed": "playwright test --headed",
    "e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0"
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
